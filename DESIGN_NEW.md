# 新签到机制设计 - 简化版本

## 问题分析

原来的签到窗口机制存在以下问题：

1. **时间规律性重复**：使用相同的随机种子导致同一时间段多次出现相同的分钟:秒钟
   - 示例：`08:43` 在 2026-02-22～2026-02-26 间重复出现
   - 示例：`06:11` 在多个日期间重复

2. **过度复杂**：
   - 复杂的历史时间避免算法
   - 需要维护 `TargetSignDate` 和 `TargetSignTime` 状态
   - 每日需要生成目标时间并在15分钟窗口内等待

3. **可检测性**：
   - 规律的签到窗口机制容易被反爬虫系统识别

## 新设计思路

移除签到窗口机制，改为**简单的每日一次签到**：

### 核心特点

1. **简化逻辑**：
   - 移除 `TargetSignTime` 和 `TargetSignDate` 
   - 不再需要预定义签到时间窗口
   - 每次定时检查流程都可尝试签到

2. **避免重复**：
   - 保留 `SleepWithJitter()` 在签到执行时添加随机抖动（0-300秒）
   - 每次实际执行签到前增加随机延迟，确保秒数变化
   - 签到时间 = **检查触发时间 + 随机抖动（0-300秒）**

3. **防检测**：
   - 随机延迟使得签到秒数不会重复
   - 依赖系统的 `CHECK_INTERVAL` 实现自然的时间分散
   - **签到时间主要在 08:00-22:00** 范围内，避免深夜/凌晨签到引起注意
   - **保证每日必签**：如果到晚上 22:00 后仍未签到，会立即执行避免遗漏

### 执行流程

```
每个 CHECK_INTERVAL 执行一次：
  ├─ 检查是否已在今天签到过
  ├─ 如果是 → 跳过
  ├─ 检查当前时间：
  │   ├─ 00:00-07:59 (凌晨/早上) → 跳过，等待进入窗口
  │   ├─ 08:00-21:59 (正常窗口) → 继续执行
  │   └─ 22:00-23:59 (深夜窗口) → 显示警告但继续执行（避免遗漏）
  └─ 执行签到流程：
     ├─ 节流检查（避免频繁重试）
     ├─ 记录尝试时间
   ├─ 生成0-300秒的随机抖动
     ├─ 执行签到
     └─ 保存结果到 sign_history
```

## 实现变更

### domain/state.go

**移除了**：
- `TargetSignDate` - 目标签到日期
- `TargetSignTime` - 目标签到时间

**添加了**：
- `SignHistory` - 最近 14 天签到记录

### scheduler/window.go

**删除了**：
- `GenerateSmartSignTime()` - 复杂的智能时间生成算法
- `scoreSignTime()` - 历史时间评分函数

**添加了**：
- `UpdateSignHistory()` - 维护签到历史记录

### signer/signer.go

**简化的 `AttemptSign()`**：
- 移除窗口检查逻辑
- 移除目标时间生成和对比
- 只需检查：是否已签到 + 节流检查
- **新增**：时间范围检查（08:00-22:00）

**修改的 `ForceSign()`**：
- 添加时间范围检查（会显示警告但仍执行）

**简化的 `logNextSignInfo()`**：
- 显示明天日期和"随机时间"提示
- 不再计算具体的预期时间

## 签到时间特点

### 原机制的问题数据
```
2026-02-26 17:08:43
2026-02-25 05:08:43  ← 重复 08:43
2026-02-24 02:08:43  ← 重复 08:43
2026-02-23 11:08:43  ← 重复 08:43
2026-02-22 10:08:43  ← 重复 08:43
```

### 新机制的优势

由于每次签到执行前都会添加 0-300 秒的随机抖动，即使多个日期的签到涉及相同的时间检查间隔，秒数也会不同。

同时，签到时间被限制在 **08:00-22:00** 范围内，避免凌晨或深夜签到行为，更符合真实用户习惯。

```
预期改进后的数据（示例）：
2026-02-26 17:08:12  ← 秒数随机，在 08:00-22:00 内
2026-02-25 10:23:47  ← 不同的秒数和时间
2026-02-24 15:42:25  ← 不同的秒数和时间
2026-02-23 19:18:59  ← 不同的秒数和时间
2026-02-22 08:35:03  ← 不同的秒数和时间
```

## 配置说明

### 相关配置变量
- `CHECK_INTERVAL` - 定期检查间隔（默认 30 分钟）
  - 决定了每次尝试签到的频率
  - 签到会在每个间隔内自动尝试（如果今天还没签到）

### 签到时间限制
- **优先时间范围**：08:00 - 21:59（正常窗口）
  - 在此时间段内正常执行自动签到
- **补救时间范围**：22:00 - 23:59（深夜窗口）
  - 如果今日尚未签到，显示警告但仍会执行，避免遗漏
- **等待时间范围**：00:00 - 07:59（凌晨/早上）
  - 跳过并等待进入正常窗口，不会在凌晨签到
- **保证每日必签**：通过深夜补救窗口确保不会因时间限制而漏签

### 移除的配置
以下配置不再需要（原来用于签到窗口）：
- ~~`DYNAMIC_WINDOW_START`~~
- ~~`DYNAMIC_WINDOW_END`~~
- ~~`DYNAMIC_WINDOW_SPAN`~~

## 行为说明

| 情况 | 时间范围 | 行为 |
|------|---------|------|
| **自动签到** | 08:00-21:59 | ✅ 正常执行签到（优先窗口） |
| **自动签到** | 22:00-23:59 且今日未签到 | ⚠️ 显示警告并执行（避免遗漏） |
| **自动签到** | 22:00-23:59 且已签到 | ⏭️ 跳过 |
| **自动签到** | 00:00-07:59 | ⏭️ 跳过并等待进入窗口 |
| **强制签到** | 任何时间 | ⚠️ 显示警告但仍执行 |

### 设计要点

1. **优先窗口**（08:00-21:59）：大部分签到在此时间段完成，符合正常使用习惯
2. **补救机制**（22:00-23:59）：如果因程序启动时间等原因错过优先窗口，深夜会自动补签
3. **凌晨保护**（00:00-07:59）：不在凌晨签到，等待早上进入窗口，更自然
4. **保证每日必签**：通过补救机制确保只要程序在运行，每天必定会签到一次

## 向后兼容性

- 旧的 `state_<id>.json` 文件仍然可以加载
- `TargetSignDate` 和 `TargetSignTime` 字段会被忽略
- 签到历史 `SignHistory` 继续被保留和使用

## 总结

新设计通过**去复杂化**和**集成随机性**来解决原有的问题：

| 方面 | 原设计 | 新设计 |
|------|-------|-------|
| 时间规律性 | 高（同分钟重复） | 低（秒数随机） |
| 时间范围 | 全天候（00:00-23:59） | 智能窗口（优先 08:00-21:59，补救 22:00-23:59） |
| 每日必签保证 | 无保证 | 通过补救窗口保证 |
| 复杂度 | 高 | 低 |
| 状态管理 | 复杂 | 简单 |
| 可维护性 | 低 | 高 |
| 防检测能力 | 一般 | 更好 |
